1)composer create-project --prefer-dist laravel/laravel Name of Project
2)php artisan serve
Create Controller
php artisan make:controller ControllerName

php artisan optimize
php artisan route:list
php artisan config:clear = every may binago
php artisan migrate - to connect the database to laravel/
		    - changes in database laravel

create Migration:
php artisan make:migration create_tablename_table
php artisan migrate:fresh = when there is changes in laravel database
php artisan migrate --path=/database/migrations/2021_05_24_225441_create_employees_table.php
rm -rf database/migrations/* = remove migrations

Git
1) git init
2) git remote add origin https://github.com/abenojardev/army-batch-02-payroll.git
3) git push -u origin main

git remote add origin https://github.com/haogen/payroll.git
git remote remove origin
rm -rf .git = removed the initialized
git remote -v = verify the git remote
git add .
git commit -m "first commit" = comments
git push --set-upstream origin master = push the files to github repo
git config --global user.name “CastroGenelyn”

Seeder
php artisan make:seeder TablenameSeeder
php artisan db:seed --class=NameofDBSeeder = upload data to database table
php artisan tinker

Models
php artisan make:model ModelName
